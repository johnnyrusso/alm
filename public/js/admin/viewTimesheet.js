$(function() {
	let timesheet = '<%-timesheetRows%>';
    let daysWorked =  '<%-daysWorked%>';
  
  if($('#' + day + '_' + item.location).length !== 0) {
	$('#' + day + '_' + item.location).append('f')
  }
})